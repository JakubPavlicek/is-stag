{{- if .Values.database.credentials }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.name }}-redis-secret
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  REDIS_PASSWORD: {{ .Values.redis.credentials.password | b64enc | quote }}
{{- end }}
