# Encrypted secrets to be deployed to the cluster
# This should be managed via sops or similar secret management tool
# Format:
#   secrets:
#     secret-name:
#       key: base64-encoded-value
secrets: {}
