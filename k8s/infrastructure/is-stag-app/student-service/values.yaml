name: student-service
replicaCount: 1
deployment:
  annotations:
    instrumentation.opentelemetry.io/inject-java: "true"
image:
  repository: jakubpavlicek/student-service
  tag: 0.0.1
service:
  ports:
    http: 8030
    grpc: 9030
database:
  enabled: true
probes:
  readiness:
    path: /actuator/health/readiness
    initialDelaySeconds: 50
    periodSeconds: 20
  liveness:
    path: /actuator/health/liveness
    initialDelaySeconds: 50
    periodSeconds: 20