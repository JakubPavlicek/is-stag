apiVersion: apps/v1
kind: Deployment
metadata:
  name: study-plan-service
  namespace: is-stag
  labels:
    app: study-plan-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: study-plan-service
  template:
    metadata:
      name: study-plan-service
      namespace: is-stag
      labels:
        app: study-plan-service
    spec:
      containers:
        - name: study-plan-service
          image: jakubpavlicek/study-plan-service:0.0.1
          imagePullPolicy: Always
          ports:
            - containerPort: 8050
              protocol: TCP
              name: http
            - containerPort: 9050
              protocol: TCP
              name: grpc
          env:
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                configMapKeyRef:
                  key: SPRING_DATASOURCE_URL
                  name: is-stag-database-config
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  key: SPRING_DATASOURCE_USERNAME
                  name: is-stag-database-secret
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: SPRING_DATASOURCE_PASSWORD
                  name: is-stag-database-secret

---

apiVersion: v1
kind: Service
metadata:
  name: study-plan-service
  namespace: is-stag
spec:
  selector:
    app: study-plan-service
  ports:
    - protocol: TCP
      port: 8050
      targetPort: 8050
      name: http
    - protocol: TCP
      port: 9050
      targetPort: 9050
      name: grpc
  type: ClusterIP
