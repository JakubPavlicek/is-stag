get:
  tags:
    - persons
  summary: Get person
  description: Retrieve a person by their unique identifier.
  operationId: getPersonProfile
  parameters:
    - $ref: '../../parameters/path/personId.yaml'
  responses:
    '200':
      description: Person details retrieved successfully
      content:
        application/json:
          schema:
            $ref: '../../schemas/PersonProfile.yaml'
    '404':
      $ref: '../../responses/404NotFound.yaml'
    '500':
      $ref: '../../responses/500InternalServerError.yaml'