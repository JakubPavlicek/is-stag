get:
  tags:
    - persons
  summary: Get person contacts
  description: Retrieve contacts associated with a person by their unique identifier.
  operationId: getPersonContacts
  parameters:
    - $ref: '../../parameters/path/person_id.yaml'
  responses:
    '200':
      description: Person contacts retrieved successfully
      content:
        application/json:
          schema:
            type: array
            items:
              type: string
    '404':
      $ref: '../../responses/404NotFound.yaml'
    '500':
      $ref: '../../responses/500InternalServerError.yaml'