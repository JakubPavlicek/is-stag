get:
  tags:
    - persons
  summary: Get person addresses
  description: Retrieve addresses associated with a person by their unique identifier.
  operationId: getPersonAddresses
  parameters:
    - $ref: '../../parameters/path/personId.yaml'
  responses:
    '200':
      description: Person addresses retrieved successfully
      content:
        application/json:
          schema:
            $ref: '../../schemas/PersonAddresses.yaml'
    '404':
      $ref: '../../responses/404NotFound.yaml'
    '500':
      $ref: '../../responses/500InternalServerError.yaml'