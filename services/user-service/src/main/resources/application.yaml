spring:
  application:
    name: user-service
  config:
    import: optional:configserver:http://config-server:8888

  data:
    redis:
      host: redis
      port: 6379
#      username: user
#      password: password
  cache:
    type: redis
    redis:
      time-to-live: 60000
      enable-statistics: true
