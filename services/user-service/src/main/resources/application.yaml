spring:
  application:
    name: user-service
  config:
    import: optional:configserver:http://config-server:8888

logging:
  level:
    com.stag.identity.shared.config.RequestLoggingFilterConfig: DEBUG